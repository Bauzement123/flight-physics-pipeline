# Project Coding and Documentation Conventions

This document establishes the project-wide standards for directory layouts, file suffixes, physical units, programming paradigms, and documentation conventions within the Flight Physics Pipeline.

---

## 1. Directory and Dataset Naming Conventions

All modules must interact with data stored under the root `data/` directory using standard subfolders:

```text
data/
├── flight_registry/       # Master lists, global registries, and summary index files
├── flight_lists/          # Sliced flight list parquet files (e.g., corridor pairs)
├── trajectories/          # Raw coordinates folder, structured by run directories
├── weather/               # Local cache of ERA5 reanalysis NetCDF (.nc) files
├── synthesized_paths/     # Idealed and temporal-gridded DTW trajectory outputs
└── results/               # Final simulated outcome datasets
```

### Dataset Subfolder Naming Schema
To prevent different cross-validation cohorts from overwriting each other, run folders inside `data/trajectories/` and `data/results/` must use the dynamic hashing naming template:
`ranks_[lower]to[upper]_strat_[strategy]_val_[val]_seed_[seed]_start_[start]_end_[end]_type_[typecode]_[hash_suffix]`
* Example: `ranks_1-5_strat_fixed_val_50_seed_42_format_roundtrip_a9f1`

---

## 2. File Suffix and Vocabulary Standards

To easily identify a dataset's processing state, files must adhere to the following naming suffixes:

| File Suffix | Description | Format |
|---|---|---|
| `*_raw.parquet` | Raw waypoints fetched from OpenSky (contains coordinate noise, outliers, gaps). | Parquet |
| `*_clean_si.parquet` | Smoothed and resampled trajectories processed by the EKF (Extended Kalman Filter). | Parquet |
| `*_synthesized_c[ID].parquet` | Idealized route centroids generated by DTW clustering, mapped on a uniform temporal grid. | Parquet |
| `*_simulated.parquet` | Trajectories containing PSFlight performance and CoCiP contrail simulation outputs. | Parquet |
| `*.nc` | ERA5 reanalysis hourly meteorological and surface radiation datasets. | NetCDF |

---

## 3. Physical Units and Coordinate Standards

The pipeline transitions between **Aviation Units** (input/raw data) and **SI Units** (internal processing, Kalman filtering, and physics simulations):

| Parameter | Aviation Units (Raw) | SI Units (Internal/Sim) | Transformation |
|---|---|---|---|
| **Altitude** | Feet (ft) | Meters (m) | $1 \text{ ft} \approx 0.3048 \text{ m}$ |
| **Speed** | Knots (kt) | Meters per second (m/s) | $1 \text{ kt} \approx 0.5144 \text{ m/s}$ |
| **Distance** | Kilometers (km) | Meters (m) | $1 \text{ km} = 1000 \text{ m}$ |
| **Coordinates** | Degrees (WGS84) | Meters (LAEA Projection) | Transformed using `pyproj` centered at mean latitude/longitude. |
| **Time** | Local/Varying timezone | UTC (timezone-naive) | Stamped to standard UTC. |

---

## 4. Coding Standards and Paradigms

* **Functional Programming**: Scripts should favor pure, stateless functions. Avoid complex state-holding classes unless wrapped around third-party APIs.
* **Global Configuration**: Shared variables (e.g., vertical levels, weather variables lists, grid resolutions) must be imported from the centralized `src/common/config.py` rather than defined locally.
* **Logging Setup**: 
  - Call `logging.basicConfig()` only inside the `if __name__ == "__main__":` block to avoid global log-format pollution when modules are imported.
  - Mirrored file logs must be saved as `extraction.log` or `simulation.log` in the active run directories.
* **CLI Invocation**: All script runs must use Python's module format:
  `python -m src.folder.script_name --args`

---

## 5. Module README Documentation Convention

Every subdirectory in `src/` must contain a documentation README (named `README.md` or `<Module_Name> README.md`) adhering to a strict 5-section structure:

1. **`# [Module Name] Module`**: Main heading and introduction of what the module solves.
2. **`## 1. Module Structure`**: Fenced text block depicting the directory structure.
3. **`## 2. Function Analysis Solution Tree (FAST)`**: A text-based tree diagram mapping sub-objectives to their code implementations, inputs, outputs, and roles.
4. **`## 3. Data Workflow`**: A Mermaid flowchart describing inputs, files read, cache databases updated, and outputs saved (including a visual rendering warning note). **Directly below the diagram, an in-depth, step-by-step text-based description of the workflow must be provided.**
5. **`## 4. CLI Usage Guide`**: Split into Bash and PowerShell syntax examples, followed by a list of parameters and allowed inputs.
6. **`## 5. Prerequisites & Dependencies`**: Highlighting required python libraries, database setups, and referencing `conventions.md`.
